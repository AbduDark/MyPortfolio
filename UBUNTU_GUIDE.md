# 🚀 دليل إعداد سيرفر Ubuntu للمشروع (React Portfolio)

هذه الخطوات هتتعمل **مرة واحدة فقط** على السيرفر بتاعك عشان نجهزه لاستقبال المشروع.

## 1. إعداد السيرفر (تثبيت Nginx)
ادخل على السيرفر بتاعك Terminal ونفذ الأوامر دي:

```bash
# تحديث النظام وتثبيت Nginx
sudo apt update
sudo apt install nginx -y

# إنشاء فولدر للمشروع (بنفس المسار اللي حددناه في GitHub Action)
sudo mkdir -p /var/www/portfolio

# إعطاء صلاحيات للكتابة في الفولدر ده (عشان الـ Action يقدر يرفع الملفات)
# استبدل 'ubuntu' باسم اليوزر بتاعك لو مختلف (تأكد من الأمر: whoami)
sudo chown -R $USER:$USER /var/www/portfolio
sudo chmod -R 755 /var/www/portfolio
```

---

## 2. إعداد Nginx لعرض الموقع
محتاجين نعرف Nginx إن أي حد يدخل على السيرفر يروح لفولدر المشروع.

1. انشئ ملف إعدادات جديد:
```bash
sudo nano /etc/nginx/sites-available/portfolio
```

2. انسخ الكود ده جوا الملف (Click Right to paste):
```nginx
server {
    listen 80;
    server_name _;  # أو اكتب الدومين بتاعك هنا لو عندك واحد

    root /var/www/portfolio;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```
*اضغط `Ctrl+X` ثم `Y` ثم `Enter` للحفظ.*

3. تفعيل الإعدادات وتشغيل Nginx:
```bash
# عمل Link للملف
sudo ln -s /etc/nginx/sites-available/portfolio /etc/nginx/sites-enabled/

# مسح الإعدادات الافتراضية (اختياري، لو عايز الموقع يشتغل على الـ IP مباشرة)
sudo rm /etc/nginx/sites-enabled/default

# التأكد من صحة الإعدادات
sudo nginx -t

# إعادة تشغيل الخدمة
sudo systemctl restart nginx
```

---

## 3. ربط GitHub بالسيرفر (SSH Keys)
عشان GitHub يقدر يدخل السيرفر ويرفع الملفات بأمان، لازم نستخدم SSH Key.

**على جهازك الشخصي (أو السيرفر، مش هتفرق، بس الأفضل إنشاء مفتاح جديد):**
```bash
ssh-keygen -t rsa -b 4096 -C "github-action"
# اضغط Enter لحد ما يخلص (بدون password)
```

هيطلعلك ملفين:
- `id_rsa` (المفتاح السري - Private)
- `id_rsa.pub` (المفتاح المعلن - Public)

**على السيرفر (Ubuntu):**
ضيف محتوى `id_rsa.pub` لملف التخويل:
```bash
nano ~/.ssh/authorized_keys
# انسخ محتوى الـ Public Key هنا واحفظ الملف
```

---

## 4. إعداد GitHub Secrets
دلوقتي روح لصفحة المشروع على **GitHub**:
1. افتح **Settings** > **Secrets and variables** > **Actions**.
2. اضغط **New repository secret**.
3. ضيف الـ 3 مفاتيح دول:

| Name | Value |
|------|-------|
| `VPS_HOST` | عنوان الـ IP الخاص بالسيرفر (مثلاً: `192.168.1.1`) |
| `VPS_USER` | اسم المستخدم في السيرفر (مثلاً: `root` أو `ubuntu`) |
| `VPS_SSH_KEY` | انسخ محتوى المفتاح السري (`id_rsa`) بالكامل هنا |

---

## 🎉 مبروك!
دلوقتي بمجرد ما تعمل `git push` للكود، الـ GitHub Action هيشتغل:
1. يبني المشروع (`npm run build`).
2. يبعت ملفات الـ `dist` للسيرفر في مسار `/var/www/portfolio`.
3. موقعك هيكون شغال ومحدث دايماً!
